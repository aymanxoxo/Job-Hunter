# ai_providers/ — user drop-zone

Drop a `*.py` file here defining **one** class that inherits
`core.ai_providers.base_provider.BaseAIProvider`. Auto-discovered at startup via `importlib`. Built-in
providers live in `core/ai_providers/`. See SDD §2.1 / §14.2.
